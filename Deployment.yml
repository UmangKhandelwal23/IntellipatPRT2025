apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy on private server
spec:
  replicas: 2
  selector:
   matchLabels:
    app: frontend
  template:
   metadata:
    labels:
     app: frontend
   containers:
    - name: mycontainer
      image: umangkhandelwal/intellipat2025:v3
      imagePullPolicy: Always
      ports:
       - containerPort: 80
